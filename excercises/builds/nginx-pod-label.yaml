---
apiVersion: v1
kind: Pod # definition
metadata:
  name : nginx # name of the pod
  labels:
     app: nginx
spec:
  containers:
  - name: nginx
    image: nginx:1.7.9
    ports:
      - containerPort: 80

